//TODO: this file should be dropped once we get testeable code in the server folder
(function() {
    'use strict';

    function sum(a, b) {
        return (a || 0) + (b || 0);
    }

    module.exports = sum;

})();